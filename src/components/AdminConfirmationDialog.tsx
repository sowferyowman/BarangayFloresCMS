import { useState } from 'react';
import { store } from '../lib/storage';

export function AdminConfirmationDialog({onConfirm,onClose}:{onConfirm:()=>void;onClose:()=>void}) { const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async(event:React.FormEvent)=>{event.preventDefault();if(await store.login('admin',password))onConfirm();else setError('Incorrect admin password.');};return <div className="modal-backdrop"><form className="modal confirm-dialog" onSubmit={submit}><div className="modal-head"><h2>Confirm Edit</h2><button type="button" className="icon-btn" onClick={onClose}>Ã—</button></div><p>Type the administrator password to edit this case record.</p>{error&&<div className="alert">{error}</div>}<label className="field"><span>Admin Password</span><input autoFocus type="password" value={password} onChange={event=>setPassword(event.target.value)} required/></label><div className="actions end"><button type="button" className="secondary" onClick={onClose}>Cancel</button><button>Confirm Edit</button></div></form></div>; }
