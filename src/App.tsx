import { useEffect,useMemo,useState } from 'react';import { BarChart3,FolderArchive,LayoutDashboard,LogOut,Plus,Settings,Users } from 'lucide-react';import type { Case } from './types';import { store } from './lib/storage';import { blankCase,matchesCaseSearch,tableColumns } from './lib/case-utils';import { CaseEditor } from './components/CaseEditor';import { AdminConfirmationDialog } from './components/AdminConfirmationDialog';import { Logo,SkeletonDashboard } from './components/common';import { Login } from './pages/Login';import { Dashboard } from './pages/Dashboard';import { Cases } from './pages/Cases';import { Residents } from './pages/Residents';import { Reports } from './pages/Reports';import { SettingsPage } from './pages/Settings';import './styles.css';
type Page='Dashboard'|'Residents'|'Cases'|'Reports'|'Settings';
export default function App(){const[auth,setAuth]=useState(sessionStorage.getItem('bf-auth')==='yes'),[page,setPage]=useState<Page>('Dashboard'),[cases,setCases]=useState<Case[]>([]),[loading,setLoading]=useState(true),[edit,setEdit]=useState<Case|null>(null),[pendingEdit,setPendingEdit]=useState<Case|null>(null),[query,setQuery]=useState(''),[visibleColumns,setVisibleColumns]=useState(()=>tableColumns.map(column=>column.id)),[columnsOpen,setColumnsOpen]=useState(false),[note,setNote]=useState(''),[darkMode,setDarkMode]=useState(localStorage.getItem('bf-dark-theme')==='on');const load=()=>{setLoading(true);return store.cases().then(setCases).finally(()=>setLoading(false));};useEffect(()=>{load();},[]);useEffect(()=>{const keydown=(event:KeyboardEvent)=>{if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==='k'){event.preventDefault();setPage('Cases');setTimeout(()=>document.querySelector<HTMLInputElement>('.search input')?.focus(),0);}if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==='n'){event.preventDefault();setEdit(blankCase());}if(event.key==='Escape'){setEdit(null);setPendingEdit(null);}};window.addEventListener('keydown',keydown);return()=>window.removeEventListener('keydown',keydown);},[]);const save=async(item:Case)=>{await store.saveCase(item);load();setNote('Case record saved successfully.');},del=async(id:number)=>{if(confirm('Delete this case record?')){await store.deleteCase(id);load();}},filtered=useMemo(()=>cases.filter(item=>matchesCaseSearch(item,query)),[cases,query]);if(!auth)return <Login ok={()=>{sessionStorage.setItem('bf-auth','yes');setAuth(true);location.reload();}}/>;const body=page==='Dashboard'?(loading?<SkeletonDashboard/>:<Dashboard cases={cases}/>):page==='Cases'?<Cases query={query} setQuery={setQuery} data={filtered} edit={setPendingEdit} del={del} visibleColumns={visibleColumns} setVisibleColumns={setVisibleColumns} columnsOpen={columnsOpen} setColumnsOpen={setColumnsOpen} add={()=>setEdit(blankCase())} loading={loading}/>:page==='Residents'?<Residents/>:page==='Reports'?<Reports cases={cases}/>:<SettingsPage note={setNote} darkMode={darkMode} setDarkMode={setDarkMode}/>;const navigation:[Page,typeof LayoutDashboard][]=[['Dashboard',LayoutDashboard],['Residents',Users],['Cases',FolderArchive],['Reports',BarChart3],['Settings',Settings]];return <div className={`app ${darkMode?'dark-mode':''}`}><aside><div className="brand"><Logo small/><span>Barangay Flores<small>Case Management</small></span></div><nav>{navigation.map(([label,Icon])=><button key={label} className={page===label?'active':''} onClick={()=>setPage(label)}><Icon size={19}/>{label}</button>)}</nav><button className="logout" onClick={()=>{sessionStorage.clear();setAuth(false);location.reload();}}><LogOut size={18}/> Sign out</button></aside><main className="content"><header><div><p className="crumb">Home / {page}</p><h1>{page}</h1></div>{page==='Dashboard'&&<button onClick={()=>setEdit(blankCase())}><Plus size={17}/> New Case</button>}</header>{note&&<div className="notice">{note}</div>}{body}</main>{pendingEdit&&<AdminConfirmationDialog onClose={()=>setPendingEdit(null)} onConfirm={()=>{setEdit(pendingEdit);setPendingEdit(null);}}/>}{edit&&<CaseEditor item={edit} save={save} close={()=>setEdit(null)}/>}</div>;}
